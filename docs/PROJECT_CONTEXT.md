# Project Context

**Status:** initial — fill this in before asking an agent to build anything nontrivial.
**Last reviewed:** [date]

This describes what this project **is, today** — not the vision, not the roadmap. An agent should be able to read this once and understand the project without you re-explaining it every session. Update it in the same change that changes what's true; a stale context doc is worse than none, because it looks authoritative.

---

## Problem

What problem does this solve, for whom, and what does it cost them today if it stays unsolved? [Replace this line.]

## Target user

Who actually uses this, and in what situation? Be specific enough that a stranger could recognize this person, not a category like "businesses." [Replace this line.]

## Value proposition

Why this, instead of what they're doing now? [Replace this line.]

## Core workflow

The main path a user takes, start to finish — the thing this project has to get right. [Replace this line.]

## Major entities

The nouns this project manages (a customer, an order, a document — whatever they are here). This is the real domain model an agent should recognize and reuse, not invent its own names for. [Replace this line.]

## Capabilities today

What's actually built and working right now. Not planned, not "basically done" — working. This is the section most likely to drift out of date; keep it honest. [Replace this line.]

## Non-goals

What this deliberately will not do. This one section prevents more scope drift than any other in this file — an agent that doesn't know the boundary will eventually build past it because nothing told it not to. [Replace this line.]

## Assumptions

What's being taken as true but hasn't actually been verified. Write these down so they can be checked later instead of silently becoming load-bearing. [Replace this line.]

## Open questions

What needs an answer before certain work can proceed. [Replace this line.]
